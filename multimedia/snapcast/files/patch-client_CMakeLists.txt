--- client/CMakeLists.txt.orig	2021-12-22 17:40:36 UTC
+++ client/CMakeLists.txt
@@ -53,6 +53,13 @@ elseif(NOT ANDROID)
         list(APPEND CLIENT_LIBRARIES ${PULSE_LIBRARIES})
         list(APPEND CLIENT_INCLUDE ${PULSE_INCLUDE_DIRS})
     endif (PULSE_FOUND)
+
+    if (SNDIO_FOUND)
+        list(APPEND CLIENT_SOURCES player/sndio_player.cpp)
+        list(APPEND CLIENT_LIBRARIES ${SNDIO_LIBRARIES})
+        list(APPEND CLIENT_INCLUDE ${SNDIO_INCLUDE_DIRS})
+    endif (SNDIO_FOUND)
+
 endif (MACOSX)
 
 if (ANDROID)
